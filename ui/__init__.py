"""PyQt5 widgets for PTMGenerator2.

These drive the Qt-free logic in the `core` package: they own the widgets, the
timer and the dialogs, and delegate the capture policy, serial protocol, light
geometry and file formats to core.
"""
